# Exact stable pin (BLD-19). Every hash-producing build -- dev machine, all three CI
# runners -- compiles with the same rustc, so a codegen change cannot masquerade as a
# desync. Bump deliberately, on every platform at once, and re-run the golden tests.
[toolchain]
channel = "1.98.1"
components = ["rustfmt", "clippy"]

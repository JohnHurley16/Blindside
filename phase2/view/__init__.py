"""The window. Belief only, except for one labelled reveal after a run is over.

`backend.py` and `palette.py` are copied from Phase 1 rather than imported: Phase 1
is a finished throwaway and this one must not depend on it.
"""

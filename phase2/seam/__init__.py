"""The JSON the induction speaks, as Python records.

`FORMAT.md` in `crates/blindside-induct` is the contract; these are its shapes on
this side of it. Nothing here knows a block id: every id in them arrives as a
string that came from blocks.json.
"""

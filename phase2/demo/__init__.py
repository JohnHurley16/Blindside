"""Demonstrations: a session in which something chooses at each stop, recorded.

**This package must never import `phase2.truth`.** It sees a run only through
`RunDriver`, and the chooser sees only a `StopView`.
"""
